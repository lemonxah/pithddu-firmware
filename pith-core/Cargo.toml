[package]
name = "pith-core"
version = "0.9.5"
edition = "2021"
description = "Pure-logic core for pithddu firmware: telemetry parse + wire formatting + field registry. Host-testable, no esp deps."
license = "MIT"

[dependencies]

[build-dependencies]
serde_json = "1"
